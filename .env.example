# Passcode for the /admin studio CMS. Change this before going live.
# Note: this is a client-side gate only (no server/DB in this iteration),
# so treat it as a soft lock, not real security.
NEXT_PUBLIC_ADMIN_PASSCODE=pimstudio
